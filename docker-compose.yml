version: '3'

services:
  kaleidos-frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: kaleidos-frontend
    ports:
      - '9002:80'
    environment:
      - EMBER_ENV=development
      - VIRTUAL_HOST=jenkins.kanselarij-dev.s.redpencil.io
      - VIRTUAL_PORT=9002
    networks:
      - proxy
      - default
    restart: always

networks:
  proxy:
    external:
      name: letsencrypt_default
