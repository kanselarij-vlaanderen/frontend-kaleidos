<div
  class="vlc-navbar vlc-navbar--bordered-bottom vlc-navbar--bg-alt vlc-navbar--large"
>
  <div class="vlc-toolbar">
    <div class="vlc-toolbar__left">
      <div class="vlc-toolbar__item">
        <h2 class="vlc-toolbar__title">
          {{await title}}
        </h2>
        <div class="vlc-page-header__sub">
          <div class="vlc-toolbar vlc-toolbar--auto">
            <div class="vlc-toolbar__left">
              <div class="vlc-toolbar__item">
                <div class="vl-u-display-flex vl-u-flex-align-center">
                  <a
                    class="vl-link vl-link--bold"
                    {{action "navigateBackToAgenda"}}
                  >
                    <i
                      class="vl-link__icon vl-link__icon--before vl-vi vl-vi-arrow-left-fat"
                    ></i>
                    {{t "buttons.back"}}
                  </a>
                  <p class="vlc-page-header__status">
                    {{t "agendaitem-details.agenda"}}
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="vlc-toolbar__right">
        <div class="vlc-toolbar__item">
          <div class="vl-action-group">
            <button
              class="vl-button vl-button--tertiary vl-button--icon-before vl-button--narrow"
              {{action "print"}}
            >
              <i
                class="vl-button__icon vl-button__icon--before vl-vi vl-vi-printer-view"
                aria-hidden="true"
              ></i>
              {{t "print"}}
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="vlc-scroll-wrapper__body">
  {{agenda/press-agenda
    title=(await title)
    currentSession=(await model.currentSession)
    agendaitemGroups=(await model.agendaitemGroups)
  }}
</div>