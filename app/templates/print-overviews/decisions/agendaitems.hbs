<div class="vl-u-spacer-extended-l">
  <div class=""></div>
  {{#utils/agendaitem-table
    model=model
    sort=sort
    page=page
    filter=filter
    include=include
    size=size
    columns=columns as |row|
  }}
    {{#if (is-pending row.decisions)}}
      {{web-components/vl-loader text=(t "decisions-loading-text")}}
    {{else}}
      <div class="vl-u-spacer-extended-l">
        <div class="vl-u-spacer">
          <div class="vl-action-group vl-action-group--align-center">
            <button class="vl-button vl-button--secondary vl-button--narrow vl-button--icon-before"
              {{action "addDecision" row}}
            >
              <i class="vl-button__icon vl-button__icon--before vl-vi vl-vi-add"
              ></i>
              {{t "add-decision"}}
            </button>
          </div>
        </div>
        {{#each (await row.decisions) as |decision|}}
          {{agenda/agendaitem/agendaitem-decision
            subcase=(await row.subcase)
            isTableRow=true
            decision=decision
            agendaitem=row
          }}
        {{/each}}
      </div>
    {{/if}}
  {{/utils/agendaitem-table}}
</div>
{{outlet}}