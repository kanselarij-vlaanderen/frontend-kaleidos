<div class="auk-scroll-wrapper__body">
  <div class="auk-u-responsive-margin">
    <table
      data-test-route-newsletter-data-table
      class="auk-table auk-table--striped"
    >
      <thead>
        <tr></tr>
        <tr>
          <Utils::ThSortable
            data-test-route-newsletter-header-number
            @currentSorting={{this.sort}}
            @field="number"
            @label="#"
            @onChange={{fn (mut this.sort)}}
          />
          <th class="auk-table__col--7">
            {{t "title-and-content"}}
          </th>
          <th>{{t "minister"}}</th>
          {{! Note: We are sorting on two keys below. In-newsletter property changes order,
                      while we want number to stay ordered ascending }}
          <Utils::ThSortable
            data-test-route-newsletter-header-show-in-newsletter
            @currentSorting={{this.sort}}
            @field="treatment.news-item.in-newsletter,number"
            @label={{t "show-in-newsletter"}}
            @onChange={{fn (mut this.sort)}}
          />
          <Utils::ThSortable
            data-test-route-newsletter-header-latest-modified
            @currentSorting={{this.sort}}
            @field="treatment.news-item.modified"
            @label={{t "latest-modified"}}
            @onChange={{fn (mut this.sort)}}
          />
          <th class="c-table__header c-table__header--actions"></th>
        </tr>
      </thead>
      <tbody>
        {{#each this.model as |item|}}
          <NewsItem::TableRow
            @agendaitem={{item.agendaitem}}
            @newsItem={{item.newsItem}}
            @onSave={{perform this.saveNewsItem}}
            @onCancel={{this.cancelEdit}}
          />
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
{{outlet}}
