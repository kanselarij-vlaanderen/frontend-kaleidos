{{newsletter/newsletter-header
  agenda=(await model.agenda)
  agendaitems=(await model.agendaitems)
}}
<div class="vl-u-spacer-extended-l">
  {{#each (await model.agendaitems) as |agendaitem|}}
    {{newsletter/newsletter-item agenda=(await model.agenda) agendaitem=(await agendaitem)}}
  {{/each}}
</div>