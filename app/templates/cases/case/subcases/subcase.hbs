{{#if @model.subcase.shortTitle}}
  {{page-title @model.subcase.shortTitle}}
{{else if @model.subcase.title}}
  {{page-title @model.subcase.title}}
{{else}}
  {{page-title (capitalize @model.subcase.subcaseName)}}
{{/if}}

{{outlet}}
