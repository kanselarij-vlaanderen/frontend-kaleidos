{{page-title (t "new-submission")}}
<Cases::NewSubmission
  @decisionmakingFlow={{@model}}
  @disableEdit={{true}}
  @submitter={{this.submitter}}
  @mandatees={{array this.submitter}}
  @onCancel={{this.onCancelSubmission}}
/>
