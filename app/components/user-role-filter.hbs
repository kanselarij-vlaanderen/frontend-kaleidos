{{#if this.loadRoles.isRunning}}
  <Auk::Loader />
{{else}}
  <Auk::Label>{{t "filter-on-rights"}}</Auk::Label>
  {{#each this.roles as |role|}}
    <AuControlCheckbox
      @label={{role.label}}
      @checked={{includes role this.selected}}
      @onChange={{fn this.toggleRole role}}
    />
  {{/each}}
{{/if}}