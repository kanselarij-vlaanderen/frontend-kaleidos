{{#if (and @simplifiedOptions @showSimplifiedToggle)}}
  <AuToggleSwitch
    @checked={{eq @selected.uri this.confidentialUri}}
    @onChange={{this.toggleConfidential}}
    ...attributes
  />
{{else}}
  <Utils::ModelSelector
    @modelName="concept"
    @selected={{@selected}}
    @disabled={{@disabled}}
    {{! clearing access-level should be avoided except legacy and publications}}
    @allowClear={{@allowClear}}
    @searchField={{@searchField}}
    @placeholder={{@placeholder}}
    @onChange={{@onChange}}
    @displayField={{@displayField}}
    @sortField={{@sortField}}
    @includeField={{@includeField}}
    @multiple={{@multiple}}
    @filterOptions={{@filterOptions}}
    @filter={{this.filter}}
    ...attributes
  />
{{/if}}