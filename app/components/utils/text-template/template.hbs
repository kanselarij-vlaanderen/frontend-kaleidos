<div class="vlc-input-field vlc-input-field-block">
  {{web-components/vl-form-label value="Tekst templates"}}
  {{#power-select
    options=items
    selected=selectedItems
    searchField=searchField
    search=(perform searchTask)
    oninput=(action "resetValueIfEmpty")
    onchange=(action "selectModel") as |session|
  }}
    {{session.plannedStart}}
  {{/power-select}}
</div>
<div class="vlc-input-field vlc-input-field-block">
  {{web-components/vl-form-label value="Tekst templates"}}
  <textarea
    rows={{rows}}
    class="vl-textarea vl-textarea--block"
    value={{text}}
	></textarea>
</div>