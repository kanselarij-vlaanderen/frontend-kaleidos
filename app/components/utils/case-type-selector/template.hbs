{{#power-select
	class="vl-select vl-select--block"
  options=caseTypes
  selected=selectedCaseType
  searchEnabled=false
  onchange=(action "chooseCaseType") 
as |caseType|
}}
  {{caseType.label}}
{{/power-select}}