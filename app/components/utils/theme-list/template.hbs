{{#power-select-multiple
  options=themes
  selected=selectedThemes
  searchField="label"
  search=(perform searchTheme)
  oninput=(action "resetValueIfEmpty")
  onchange=(action "chooseTheme") 
as |theme|
}}
  {{theme.label}}
{{/power-select-multiple}}