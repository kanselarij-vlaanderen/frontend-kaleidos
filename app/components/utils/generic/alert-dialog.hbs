<Auk::Modal @size="small">
  <Auk::Modal::Body>
    <h3 class="auk-h3">{{@title}}</h3>
    <Utils::LoadableContent
      @isLoading={{this.isLoading}}
      @reserveSpace={{true}}
    >
      {{@message}}
    </Utils::LoadableContent>
  </Auk::Modal::Body>
  <Auk::Modal::Footer
    @bordered={{false}}
    @cancelDisabled={{this.isLoading}}
    @onCancel={{perform this.cancel}}
  >
    <AuButton
      data-test-alert-dialog-confirm
      @skin="primary"
      @alert={{this.isAlertConfirm}}
      @icon={{this.confirmIcon}}
      @disabled={{this.isLoading}}
      @loading={{this.confirm.isRunning}}
      {{on "click" (perform this.confirm)}}
    >
      {{this.confirmText}}
    </AuButton>
  </Auk::Modal::Footer>
</Auk::Modal>