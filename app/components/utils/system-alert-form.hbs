<div class="au-c-form">
  <AuFormRow data-test-system-alert-form-begin-date>
    <AuLabel>{{t "start-date"}}</AuLabel>
    <Auk::Datepicker
      @enableTime={{true}}
      @date={{this.beginDate}}
      @onChange={{this.selectBeginDate}}
    />
  </AuFormRow>
  <AuFormRow data-test-system-alert-form-end-date>
    <AuLabel>{{t "end-date"}}</AuLabel>
    <Auk::Datepicker
      @enableTime={{true}}
      @date={{this.endDate}}
      @onChange={{this.selectEndDate}}
    />
  </AuFormRow>
  <AuFormRow data-test-system-alert-form-title>
    <AuLabel>{{t "title"}}</AuLabel>
    <AuInput
      value={{this.alert.title}}
      @width="block"
      {{on "input" (pick "target.value" (set this "alert.title"))}}
    />
  </AuFormRow>
  <AuFormRow data-test-system-alert-form-message>
    <AuLabel for="system-message">{{t "system-message"}}</AuLabel>
    <AuTextarea
      id="system-message"
      rows="2"
      value={{this.alert.message}}
      @width="block"
      {{on "input" (pick "target.value" (set this "alert.message"))}}
    />
  </AuFormRow>
  <AuFormRow data-test-system-alert-form-severity>
    <AuLabel>{{t "severity"}}</AuLabel>
    <Utils::ModelSelector
      @modelName="alert-type"
      @searchField="label"
      @displayField="label"
      @sortField="label"
      @selected={{this.alert.type}}
      @onChange={{this.selectType}}
    />
  </AuFormRow>
</div>