<tr
  data-test-document-details-row
  class={{if @row.isToBeDeleted "auk-u-opacity--1/2"}}
>
  {{#if (and @isEditingEnabled @isDesignAgenda)}}
    <td class="auk-table-checkbox-col">
      <Auk::Checkbox
        @checked={{@isSelected}}
        {{on "input" (fn @onSelect @row)}}
      />
    </td>
  {{/if}}
  <td>
    {{#if (and @isEditingEnabled @isDesignAgenda)}}
      <Auk::Input data-test-document-details-input type="text" @block @value={{mut @row.name}} />
    {{else}}
      {{@row.name}}
    {{/if}}
  </td>
  <td>
    {{#if (and @isEditingEnabled @isDesignAgenda)}}
      <Utils::ModelSelector
        data-test-document-details-row-type
        @modelName="document-type"
        @allowClear={{true}}
        @searchField="label"
        @propertyToShow="label"
        @sortField="priority"
        @selectedItems={{@row.documentType}}
        @selectModel={{@row.setDocumentType}}
      />
    {{else}}
      {{@row.documentType.label}}
    {{/if}}
  </td>
  {{#if (and @isEditingEnabled @isDesignAgenda)}}
    <td>
      <Utils::ModelSelector
        data-test-document-details-row-access-level
        @modelName="access-level"
        @allowClear={{true}}
        @propertyToShow="label"
        @sortField="priority"
        @selectedItems={{@row.accessLevel}}
        @selectModel={{@row.setAccessLevel}}
      />
    </td>
  {{else}}
    <td>
      <Utils::Documents::FileTypePill
        @extension={{@row.piece.file.extension}}
      />
    </td>
  {{/if}}
  <td>
    <WebComponents::AuButtonToolbar>
      {{#if (not @isDesignAgenda)}}
        <Auk::Button
          @layout="icon-only"
          @icon="book"
          @skin="borderless"
          @href={{@row.piece.viewDocumentURL}}
          target="_blank"
        />
      {{else if (not @isEditingEnabled)}}
        <Auk::Button
          @layout="icon-only"
          @icon="book"
          @skin="borderless"
          @href={{@row.piece.viewDocumentURL}}
          target="_blank"
        />
      {{else if @row.isToBeDeleted}}
        <Auk::Button
          data-test-document-details-row-undo-delete
          @icon="redo"
          @layout="icon-only"
          @skin="borderless"
          {{on "click" (fn @row.setToBeDeleted false)}}
        />
      {{else}}
        <Auk::Button
          data-test-document-details-row-delete
          @icon="trash"
          @layout="icon-only"
          @skin="danger-borderless"
          {{on "click" (fn @row.setToBeDeleted true)}}
        />
      {{/if}}
    </WebComponents::AuButtonToolbar>
  </td>
</tr>