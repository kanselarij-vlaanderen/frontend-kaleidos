<div {{did-update (perform this.groupDocumentsByContainer) this.documents}}>
  {{#each this.latestDocuments as |doc|}}
    <Documents::DocumentCard
      @piece={{doc}}
      @hideNewerVersions={{true}}
      @didDeletePiece={{@didDeletePiece}}
      @didDeleteContainer={{@didDeleteContainer}}
      @onOpenUploadModal={{@onOpenUploadModal}}
      @onAddPiece={{@onAddPiece}} />
  {{else}}
    {{#if this.groupDocumentsByContainer.lastSuccessful}}
      <div class="auk-u-mt-2">
        <WebComponents::VlAlert @message={{t "no-documents-yet"}} />
      </div>
    {{/if}}
  {{/each}}
</div>