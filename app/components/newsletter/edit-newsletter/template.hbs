<div class="vl-modal-dialog__content">
  <div class="vlc-input-field-block">
    {{web-components/vl-form-label value=(t "edit-item.date-publication")}}
    {{web-components/vl-datepicker selectedDate=date dateChanged=(action "selectDate")}}
  </div>
  <div class="vlc-input-field-block">
    {{web-components/vl-form-label value=(t "title-newsletter")}}
    {{textarea rows="5" class="vl-textarea vl-textarea--block" value=selectedMeeting.newsletter.title}}
  </div>
  <div class="vlc-input-field-block">
    {{web-components/vl-form-label value=(t "subtitle-newsletter")}}
    {{textarea rows="5" class="vl-textarea vl-textarea--block" value=selectedMeeting.newsletter.subtitle}}
  </div>
  <div class="vlc-input-field-block">
    {{web-components/vl-form-label value=(t "newsletter-doc-date")}}
    {{web-components/vl-datepicker selectedDate=docDate dateChanged=(action "selectDocDate")}}
  </div>
</div>
<div class="vlc-navbar vlc-navbar--no-padding vlc-navbar--bordered-top">
  <div class="vlc-toolbar">
    <div class="vlc-toolbar__left">
      <div class="vlc-toolbar__item">
        <button
          class="vl-button vl-button--link vl-button--narrow"
          {{action "close"}}
        >
          {{t "buttons.cancel"}}
        </button>
      </div>
    </div>
    <div class="vlc-toolbar__right">
      <div class="vlc-toolbar__item">
        <button
          class="vl-button vl-button--narrow"
          {{action "saveChanges" selectedMeeting}}
        >
          {{t "buttons.save"}}
        </button>
      </div>
    </div>
  </div>
</div>