<menu>
  <div class="menu--content">
    <ul>
      <li>
        {{#link-to "agendas"}}
          <span>{{t 'navigation.agendas' }}</span>
          <div class="menu-item-active-border"/>
        {{/link-to}}
      </li>
      <li>
        {{#link-to "cases" }}
          <span>{{t 'navigation.cases' }}</span>
          <div class="menu-item-active-border"/>
        {{/link-to}}
      </li>
      <li>
        {{#link-to "settings"}}
        <span>{{t 'navigation.settings' }}</span>
        <div class="menu-item-active-border"/>
        {{/link-to}}
        </li>
      <li>
        <span>{{t 'navigation.notifications' }}</span>
        <div class="menu-item-active-border"/>
      </li>
    </ul>
  </div>
</menu>