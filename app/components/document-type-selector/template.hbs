{{#power-select
  options=documentTypes
  searchField="label"
  selected=selectedDocumentType
  searchField="description"
  onchange=(action "chooseDocumentType") as |documentType|
}}
  {{documentType.label}}
{{/power-select}}