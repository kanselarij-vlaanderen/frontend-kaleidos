<div class="vl-uploaded-document__title">
  <div class="auk-badge auk-badge--medium auk-badge--default">
    <div class="auk-icon auk-icon--default">
      <i class="ki ki-document"></i>
    </div>
  </div>
  <div class="vl-uploaded-document__title-container">
    <div class="title">
      <h6 class="auk-h4 auk-u-m-0"
        data-test-vl-uploaded-document-filename>
        {{name}}
      </h6>
      {{#if view}}
        <Auk::Button
          @skin="borderless"
          @layout="icon-only"
          @icon="book"
          {{action "view"}}
        >
          {{t "show-piece"}}
        </Auk::Button>
      {{/if}}
      {{#if download}}
        <Auk::Button
          @skin="borderless"
          @layout="icon-only"
          @icon="download"
          {{action "download"}}
        >
          {{t "download"}}
        </Auk::Button>
      {{else if downloadLink}}
        <Auk::Button
          @skin="borderless"
          @layout="icon-only"
          @icon="download"
          @href={{downloadLink}}
          download={{if downloadName downloadName name}}
        >
          {{t "download"}}
        </Auk::Button>
      {{/if}}
      {{#if delete}}
        <Auk::Button
          @skin="danger-borderless"
          @layout="icon-only"
          @icon="delete"
          {{action "delete"}}
          data-test-vl-uploaded-document-delete-piece
        >
          {{t "delete"}}
        </Auk::Button>
      {{/if}}
    </div>
    <div class="vl-document__metadata metadata">
      {{extension}}
      -
      {{format-bytesize size}}
    </div>
  </div>
</div>
{{yield}}
