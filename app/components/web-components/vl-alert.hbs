<div
  data-test-alert
  class="vl-alert {{this.typeClass}} {{if @small "vl-alert--small"}}"
  ...attributes
  role="alert"
>
  {{#if (eq @type "loading")}}
    <div class="vl-alert__loader">
      <div class="auk-loader" role="alert" aria-busy="true"></div>
    </div>
  {{else}}
    <div class="vl-alert__icon">
      <div class='auk-icon'>
        <i class="{{this.iconClass}} vl-alert__icon-padding-bottom--small" aria-hidden="true"></i>
      </div>
    </div>
  {{/if}}
  {{#if @closeable}}
    <button data-test-alert-close-button class="vl-alert__close" type="button" {{action "close"}}>
      <div class='auk-icon'>
        <i class="ki-cross" aria-hidden="true"></i>
      </div>
      <div class="auk-u-sr-only">
        {{t "close"}}
      </div>
    </button>
  {{/if}}
  <div class="vl-alert__content">
    {{#if @title}}
      <p
        data-test-alert-title
        class="vl-alert__title"
      >
        {{@title}}
      </p>
    {{/if}}
    {{#if (has-block)}}
      {{yield}}
    {{else}}
      <div
        data-test-alert-message
        class="vl-alert__message"
      >
        {{@message}}
      </div>
    {{/if}}
  </div>
</div>