{{utils/model-selector
  modelName="account-group"
  searchField="name"
  propertyToShow="name"
  sortField="name"
  selectedItems=user.group
  userGroup=(await user.group)
  selectModel=(action "chooseAccountGroup")
}}