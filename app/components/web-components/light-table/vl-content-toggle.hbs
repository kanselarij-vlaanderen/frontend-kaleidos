{{!-- TODO: octane-refactor --}}
{{!-- template-lint-disable no-action --}}
{{#if this.isLoading}}
  <Auk::Loader />
{{else}}
  <Auk::Toggle
    checked={{await this.value}}
    {{on "click" (action "valueChanged" this.row)}}
  />
{{/if}}