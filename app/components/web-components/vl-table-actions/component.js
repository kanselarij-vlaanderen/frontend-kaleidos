import Component from '@ember/component';

export default Component.extend({
	// TODO: create computed to check if there are documents with type: Nota
	// Else don't show button to open nota ofc
	actions: {
		async openDocument(row) {

		}
	}
});
