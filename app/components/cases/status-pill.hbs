{{#if this.loadStatusText.isRunning}}
  <AuLoader @inline={{true}} @hideMessage={{true}} />
{{else}}
  {{#if this.statusText}}
    <AuPill @size="small" @skin="ongoing">
      {{capitalize this.statusText}}
    </AuPill>
  {{else}}
    <p class="au-u-text-center">-</p>
  {{/if}}
{{/if}}