<form onsubmit={{action "createCase"}}>
  <div class="vl-modal-dialog__content">
    <div class="vl-u-spacer">
      <div class="vlc-input-field-block">
        {{web-components/vl-form-label value=(t "public")}}
        {{web-components/vl-toggle value=confidential}}
      </div>
    </div>
    {{web-components/vl-radio-button-list
      title=(t "case-level")
      modelName="policy-level"
      selectedModel=selectedPolicyLevel
      modelChanged=(action "policyLevelChanged")
    }}
    {{cases/forms/metadata-form
      submitter=submitter
      meeting=meeting
      shortTitle=shortTitle
      selectedPolicyLevel=selectedPolicyLevel
      selectedMeetingChanged=(action "selectedMeetingChanged")
      submitterChanged=(action "submitterChanged")
    }}
  </div>
  <div class="vlc-navbar vlc-navbar--no-padding vlc-navbar--bordered-top">
    <div class="vlc-toolbar">
      <div class="vlc-toolbar__left">
        <div class="vlc-toolbar__item">
          <button
            type="button"
            class="vl-button vl-button--link vl-button--narrow"
            {{action "close"}}
          >
            {{t "cancel"}}
          </button>
        </div>
      </div>
      <div class="vlc-toolbar__right">
        <div class="vlc-toolbar__item">
          {{web-components/vl-save-button
            text=(t "create-case")
            type="submit"
            isLoading=isLoading
          }}
        </div>
      </div>
    </div>
  </div>
</form>