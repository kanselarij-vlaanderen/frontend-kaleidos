{{#if this.data.isRunning}}
  <Auk::Loader />
{{else}}
  {{#if this.data.value.signingHubUrl}}
    <AuPill
      data-test-signature-pill
      @icon="sign"
      @skin={{this.skin}}
      @href={{if this.isClickable this.data.value.signingHubUrl}}
      target="_blank"
    >
      {{this.data.value.status.label}}
    </AuPill>
  {{else}}
    <AuPill
      data-test-signature-pill
      @icon="sign"
      @skin={{this.skin}}
      @route={{if (and this.isClickable this.data.value.route) this.data.value.route}}
      @models={{if (and this.isClickable this.data.value.models) this.data.value.models}}
    >
      {{this.data.value.status.label}}
    </AuPill>
  {{/if}}
{{/if}}