{{#if @route}}
  <LinkTo
    type="button"
    class="auk-button-link {{this.skin}} {{this.size}} {{this.block}} {{this.padded}}"
    @route={{@route}}
    @models={{this.models}}
    @disabled={{@disabled}}
    @activeClass={{@activeClass}}
    @current-when={{@current-when}}
    ...attributes
  >
    <Auk::Button::InnerLayout @layout={{@layout}} @icon={{@icon}} @size={{@size}}>{{yield}}</Auk::Button::InnerLayout>
  </LinkTo>
{{else if @href}}
  <a
    href={{@href}}
    class="auk-button-link {{this.skin}} {{this.size}} {{this.block}} {{this.padded}}"
    disabled={{@disabled}}
    ...attributes
  >
    <Auk::Button::InnerLayout @layout={{@layout}} @icon={{@icon}} @size={{@size}}>{{yield}}</Auk::Button::InnerLayout>
  </a>
{{else}}
  {{!-- When using a button-link as an {{on click}} action, you typically dont pass href or route and need to fallback on a normal button (f.e. inside dropdown)--}}
  <button
    type="button"
    class="auk-button-link {{this.skin}} {{this.size}} {{this.block}} {{this.padded}}"
    disabled={{@disabled}}
    ...attributes
  >
    <Auk::Button::InnerLayout @layout={{@layout}} @icon={{@icon}} @size={{@size}}>{{yield}}</Auk::Button::InnerLayout>
    {{#if (has-block "floater")}}
      {{yield to="floater"}}
    {{/if}}
  </button>
{{/if}}