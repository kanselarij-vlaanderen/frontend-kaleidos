import Component from '@glimmer/component';

/**
 *
 * @argument logoType {String}: ? TODO: presumably something like `brandName` would be clearer here?
 */
export default class Brand extends Component {}
