<div data-test-auk-alert-container class="auk-alert {{this.skin}}" ...attributes>
  <div class="auk-alert__body">
    <Auk::Badge @icon="{{this.icon}}" @skin={{@skin}} @size={{@size}} />
    <div class="auk-alert__text">
      {{#if @title}}
        <p data-test-auk-alert-title class="auk-alert__title">{{@title}}</p>
      {{/if}}
      <p data-test-auk-alert-message class="auk-alert__message">{{@message}}</p>
    </div>
  </div>
  {{#if @isClosable}}
    <Auk::Button data-test-auk-alert-close @skin="muted-borderless" @icon="close" @layout="icon-only" @size={{@size}}>{{t "cancel"}}</Auk::Button>
  {{/if}}
</div>