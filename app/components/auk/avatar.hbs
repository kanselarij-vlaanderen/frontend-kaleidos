{{#if (has-block)}}
  <div class="auk-avatar-and-text">
    <Auk::Avatar::Visual @size={{@size}} @icon={{@icon}} @image={{@image}} @name={{@name}} />
    <div class="auk-avatar-and-text__text">
      {{yield}}
    </div>
  </div>
{{else}}
  <Auk::Avatar::Visual @size={{@size}} @icon={{@icon}} @image={{@image}} @name={{@name}} />
{{/if}}