{{yield
  (hash
    Content=(component
      "auk/popover/content"
      targetElement=this.targetElement
      title=this.title
      placement=this.placement
      isShown=this.isShown
      hide=this.hide
      isWide=this.isWide
    )
    isShown=this.isShown
    target=this.target
  )
}}