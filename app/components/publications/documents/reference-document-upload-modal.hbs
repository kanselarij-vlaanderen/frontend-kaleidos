<Auk::Modal>
  <Auk::Modal::Header
    @title={{t
      "upload-reference-document"
      identifier=@publicationFlow.identification.idName
    }}
    @onClose={{this.cancel.perform}}
    @closeDisabled={{this.isCancelDisabled}}
  />
  <Auk::Modal::Body>
    <Auk::FileUploader
      @multiple={{true}}
      @reusable={{true}}
      @onUpload={{this.uploadPiece}}
    />
    <Publications::Publication::PublicationDocumentsList
      @pieces={{this.uploadedPieces}}
      @setReceivedDate={{this.setReceivedDate}}
      @onDeletePiece={{this.deleteUploadedPiece.perform}}
      class="au-u-margin-top-tiny"
    />
  </Auk::Modal::Body>
  <Auk::Modal::Footer
    @onCancel={{this.cancel.perform}}
    @cancelDisabled={{this.isCancelDisabled}}
  >
    <AuButton
      data-test-reference-upload-save
      @skin="primary"
      @disabled={{this.isSaveDisabled}}
      @loading={{this.save.isRunning}}
      @loadingMessage={{t "loading"}}
      {{on "click" this.save.perform}}
    >
      {{t "save"}}
    </AuButton>
  </Auk::Modal::Footer>
</Auk::Modal>