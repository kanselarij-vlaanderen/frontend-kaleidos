{{#unless this.loadData.isRunning}}
  <Utils::GovernmentAreas::GovernmentAreasPanel
    @referenceDate={{or this.meeting.plannedStart this.agenda.created @publicationFlow.created}}
    @governmentAreas={{this.governmentAreas}}
    @allowEditing={{this.getAllowEditing}}
    @onSave={{this.saveGovernmentAreas}}
    class="auk-u-mb-3"
  />
{{/unless}}