{{#unless this.loadData.isRunning}}
  <span class="auk-u-ml">
    {{#if @publicationSubcase.endDate}}
      <Auk::StatusPill data-test-proofs-statuspill-done @status="done" />
    {{else}}
      {{#if (gt this.requests.length 0)}}
        <Auk::StatusPill data-test-proofs-statuspill-in-progress />
      {{else}}
        <Auk::StatusPill
          data-test-proofs-statuspill-not-started
          @status="not-started"
        />
      {{/if}}
    {{/if}}
  </span>
{{/unless}}