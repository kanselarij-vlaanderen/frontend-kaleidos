<Auk::Modal>
  <Auk::Modal::Header
    @title={{t "publication-edit-modal-title"}}
    @onClose={{@onCancel}}
    @closeDisabled={{this.save.isRunning}}
  />
  <Auk::Modal::Body>
    <Utils::LoadableContent
      @isLoading={{this.save.isRunning}}
      @reserveSpace={{true}}
    >
      <div class="au-c-form">
        <AuFormRow>
          <AuLabel for="publicationRegisterModal_publicationDate">
            {{t "publication-date"}}
          </AuLabel>
          <Auk::Datepicker
            id="publicationRegisterModal_publicationDate"
            @date={{this.publicationDate}}
            @onChange={{this.setPublicationDate}}
          />
        </AuFormRow>
      </div>
    </Utils::LoadableContent>
  </Auk::Modal::Body>
  <Auk::Modal::Footer
    @onCancel={{@onCancel}}
    @cancelDisabled={{this.save.isRunning}}
  >
    <AuButton
      @skin="primary"
      @disabled={{this.isSaveDisabled}}
      @loading={{this.save.isRunning}}
      {{on "click" (perform this.save)}}
    >
      {{t "save"}}
    </AuButton>
  </Auk::Modal::Footer>
</Auk::Modal>