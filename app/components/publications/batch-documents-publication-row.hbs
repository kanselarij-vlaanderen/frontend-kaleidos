<tr>
  <td>
    {{@piece.name}}
  </td>
  <td>
    {{@piece.documentContainer.type.label}}
  </td>
  <td>
    <PowerSelect
      @searchEnabled={{false}}
      @selected={{this.selectedLinkModeOption}}
      @options={{this.linkModeOptions}}
      @onchange={{this.selectLinkModeOption}}
      as |option|>
      {{option.label}}
    </PowerSelect>
  </td>
  <td>
    {{#if this.selectedLinkModeOption.isEnabledLink}}
      <Publications::PublicationFlowSelector
        @selected={{@piece.publicationFlow}}
        @case={{@case}}
        @onChange={{this.selectPublicationFlow}}
        />
    {{else}}
      <Auk::Button
        @icon="add"
        {{on 'click' this.openNewPublicationModal}}>
        {{t "publications-new"}}
      </Auk::Button>
    {{/if}}
  </td>
  <td>
    <WebComponents::AuButtonToolbar>
      <Auk::Button
        @layout="icon-only"
        @icon="download"
        @skin="borderless"
        @href={{@piece.file.namedDownloadLink}}
        download
      />
      <Auk::Button
        @layout="icon-only"
        @icon="book"
        @skin="borderless"
        @href={{@piece.viewDocumentURL}}
        target="_blank"
      />
    </WebComponents::AuButtonToolbar>
  </td>
</tr>

{{#if this.isOpenNewPublicationModal}}
  <Publications::NewPublicationModal
    @case={{this.case}}
    @onSave={{this.saveNewPublication}}
    @onCancel={{this.cancelNewPublication}} />
{{/if}}