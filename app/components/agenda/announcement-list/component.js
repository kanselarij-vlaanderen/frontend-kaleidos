import Component from '@ember/component';
import { computed } from '@ember/object';

export default Component.extend({
  actions: {
    selectAnnouncement(announcement) {
      this.selectAnnouncement(announcement);
    }
  }
});
