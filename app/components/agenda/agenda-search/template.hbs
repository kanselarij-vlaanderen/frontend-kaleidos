{{#power-select
  placeholder="agenda name"
  selected=currentAgenda
	options=agendas
  searchField="name"
  onchange=(action "chooseAgenda") as |agenda|
}}
  <strong>
    {{agenda.agendaName}}
  </strong>
{{/power-select}}
