<WebComponents::VlModal
  @isOverlay={{true}}
  @title={{t "manage-signatures"}}
  @closeModal={{action "close"}}
  @large={{true}}
>
  <Utils::ManageSignatures @closeModal={{action "close"}} />
</WebComponents::VlModal>