<div class="vl-u-bg-porcelain vl-u-bg-width vlc-scroll-wrapper__body">
  <div class="auk-u-m-8">
    <div class="auk-u-flex auk-u-flex--justify-between">
      <h2 class="vl-title vl-title--h3">
        {{t "compare-title"}}
      </h2>
      <div class="auk-u-flex vl-u-flex-end">
        <div class="auk-u-ml-2">
          <a data-test-compare-show-changes href="" class="auk-button-link" {{action "toggleChangesOnly"}}>
            {{#if (not isShowingChanges)}}
              {{t "show-changes"}}
            {{else}}
              {{t "show-all"}}
            {{/if}}
          </a>
        </div>
      </div>
    </div>

    {{agenda/compare-agenda-list
      currentAgenda=(await model.currentAgenda)
      agendaToCompare=(await model.agendaToCompare)
      isShowingChanges=isShowingChanges
    }}
  </div>
</div>