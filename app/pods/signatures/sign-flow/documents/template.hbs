<Auk::Modal @size="full-screen">
  <Auk::Modal::Header @title={{this.signFlow.longTitle}} @onClose={{this.close}} />
  <Auk::Modal::Body @noPadding={{true}}>
    {{#if this.isShown}}
      {{! Known issue: sandbox mode is not possible, because the SigningHub page requires the documents cookie
        Because of this the user requires to push back button multiple times to go the previous page }}
      <iframe class="auk-u-block auk-u-maximize-height auk-u-maximize-width"
        src={{this.signingHubUrl}}
        {{on "load" this.onLoad}}>
      </iframe>
    {{else}}
      {{! Goal: Prevent user from seeing "Pagina niet gevonden" page in iFrame, when going back on the handtekenmap.vlaanderen.be page. }}
      {{! @todo Verify whether this is necessary and works. }}
      <Auk::Loader />
    {{/if}}
  </Auk::Modal::Body>
</Auk::Modal>
