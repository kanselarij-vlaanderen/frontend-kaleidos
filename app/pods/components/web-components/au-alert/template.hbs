<div class="au2-alert {{this.skin}}" ...attributes>
  <div class="au2-alert__body">
    <Badge @icon="{{this.icon}}" @skin={{@skin}}/>
    <div class="au2-alert__text">
      {{#if @title}}
        <p class="au2-alert__title">{{@title}}</p>
      {{/if}}
      <p class="c-alert__message">{{@message}}</p>
    </div>
  </div>
  {{#if @isClosable}}
    <Button @skin="tertiary" @icon="close" @layout="icon-only">{{t "close"}}</Button>
  {{/if}}
</div>