<Textarea 
  class="auk-textarea auk-textarea--block"
  data-test-auk-textarea
  rows={{this.getRows}} 
  @value={{@value}} 
  @enter={{@enter}}
  @insert-newline={{@insert-newline}}
  @escape-press={{@escape-press}}
  @focus-in={{@focus-in}}
  @focus-out={{@focus-out}}
  @key-press={{@key-press}}
/>
