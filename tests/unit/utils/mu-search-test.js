import muSearch from 'fe-redpencil/utils/mu-search';
import { module, test } from 'qunit';

module('Unit | Utility | mu-search', () => {
  // Replace this with your real tests.
  test('it works', (assert) => {
    const result = muSearch();
    assert.ok(result);
  });
});
